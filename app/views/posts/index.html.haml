%h1 Listing posts

%table
  %tr
    %th Title
    %th Description
    %th Department name
    %th Details link
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.description
      %td= post.department_name
      %td= post.details_link
      %td= link_to 'Show', post
      %td= link_to 'Edit', edit_post_path(post)
      %td= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Post', new_post_path
